<p id="notice"><%= notice %></p>



<div class="jumbotron">
  <h1 class="display-4"><%= @item.user %></h1>
  <hr>
  <p class="lead"><%= @item.note %> | <%= @item.rental_peiod %></p>
  <p class="lead">
    <%= link_to 'Edit', edit_item_path(@item), class: "btn btn-secondary" %>
    <%= link_to 'Back', items_path, class: "btn btn-dark"  %>
  </p>
</div>
